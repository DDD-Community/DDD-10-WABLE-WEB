import { CardType } from '../../types';

const CardTypeText: Record<CardType, string> = {
  CELEBRATION: '축하',
  ENCOURAGEMENT: '응원',
  GRATITUDE: '감사',
  GREETING: '인사',
};

export { CardTypeText };
